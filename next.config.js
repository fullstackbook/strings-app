/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["avatars.githubusercontent.com", "cloudflare-ipfs.com", "tl5vq6svk3vajety.public.blob.vercel-storage.com"]
  }
}

module.exports = nextConfig
